<header class="navbar navbar-default navbar-fixed-top navbar-custom affix-top">
    <div class="container">
        <nav>
            <div class="apptitle">
                <%= link_to "Pibe", root_path, :style => "color:white" %>
            </div>
            <ul class="nav navbar-nav navbar-right">
                <% if logged_in? %>
                    <li><%= link_to "log out", logout_path, method: "delete", :style => "color:white" %></li>
                <% else %>
                    <li><%= link_to "check teams", teams_path, :style => "color:white" %></li>
                    <li><%= link_to "sign up", signup_path, :style => "color:white" %></li>
                    <li><%= link_to "sign in", login_path, :style => "color:white" %></li>
                <% end %>
            </ul>
        </nav>
    </div>
</header>