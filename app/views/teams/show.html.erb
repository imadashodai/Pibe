<div class="sidebar">
    <%= render "my/show" %>
</div>
<div class="members">
    <ul>
        <% @team_users.each do |user| %>
            <li>
                <%= image_tag user.avatar.url, :alt => "noimage" %><br>
                <%= link_to user.name, user_path(user) %>
            </li>
        <% end %>
    <ul>
</div>